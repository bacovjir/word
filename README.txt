== Word ==

- Simple sentence and words processor -

building the app: mvn package
running the app: java -jar -Xmx32m word.jar

App takes type of output file 'csv' or 'xml' and path to the input file.
The default values are 'csv' and 'small.in'

The output of running the application is out.csv or out.xml file based on user inputs
